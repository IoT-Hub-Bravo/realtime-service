name: ${COMPOSE_PROJECT_NAME:-realtime-service}

services:
  realtime:
    build:
      context: ..
      dockerfile: docker/python/Dockerfile
    env_file:
      - ../.env
    ports:
      - "8002:8000"
    restart: unless-stopped
